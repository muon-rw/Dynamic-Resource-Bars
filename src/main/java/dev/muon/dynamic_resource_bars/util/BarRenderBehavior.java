package dev.muon.dynamic_resource_bars.util;

public enum BarRenderBehavior {
    VANILLA, // Show vanilla bar
    CUSTOM,  // Show custom bar (if custom settings are also enabled/valid)
    HIDDEN   // Hide both vanilla and custom bar
} 